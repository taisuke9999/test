version: 0.0

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Building the React app...
      - npm run build
      - chmod +x test.sh
      - ls -l
      - ./test.sh

artifacts:
  files:
    - "**/*"
  base-directory: "build"
